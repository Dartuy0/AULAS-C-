**incompleto:** #include <iostream>

int main(void){
	int nota, nota1, nota2, nota3, nota4, nota5;
	int vet[5];
	int media;

std::cout<<"digite a nota:";
std::cin>> nota;
std::cout<<"digite a nota:";
std::cin>> nota;
std::cout<<"digite a nota:";
std::cin>> nota;
std::cout<<"digite a nota:";
std::cin>> nota;
std::cout<<"digite a nota:";
std::cin>> nota;

vet[0] = nota;
vet[1] = nota1;
vet[2] = nota2;
vet[3] = nota3;
vet[4] = nota4;

media = (nota + nota1 + nota2 + nota3 + nota4) /2;
std::cout<<"media:", media ;

	
	return 0;


--------------------------------------------------------------------------------------------

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	
float notas[5];
float soma = 0;
float media;
int acimaDaMedia = 0;

//transformando as notas em vetores usando o notas[5] logo a pos isso somamos um por um ja que if se repete ate todas as notas serem dadas
for (int i = 0; i < 5; i++){
	printf("Digite a nota %d: ", i + 1);
	scanf("%f", &notas[i]);
	soma += notas[i];
}
media = soma / 5;//soma as notas e apos isso diveide elas
for (int i = 0; i< 5; i++) {
	if (notas[i > media]) {
		acimaDaMedia++;
	}	
}
printf("\n Media da Turma: %.2f\n", media);//ganhamos os resultados das notas e as que estao ou naoa acima da media
printf("Quantidade de notas acima da media: %d", acimaDaMedia);

	return 0;
}